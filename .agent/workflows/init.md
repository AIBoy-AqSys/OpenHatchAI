---
description: AI初期化ワークフロー。指示書を読み込み、ログを開始する。
---

1. `AI_INSTRUCTIONS.md` と `AI_PROJECT_BOOTSTRAP_PROTOCOL.md`、`README.md`、`.env` を確認しルールであれば遵守。
2. `log/` 配下に `[YYYYMMDD]_[TASK_NAME].log` 形式で作業ログを作成する（既に存在しない場合）。
3. ログに「タスクの目的」と「初期思考」を記録する。
4. このプロジェクトのローカルに対する指示か、アプリ構築用サーバに対する指示か熟慮し、不明であればユーザに問い合わせること。