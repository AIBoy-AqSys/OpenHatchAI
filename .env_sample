# =============================================================
# .env_sample - 環境変数テンプレート（公開可能）
# このファイルをコピーして .env を作成してください。
#   cp .env_sample .env
# 実際の値は .env に記入してください。.env は絶対に公開しないこと。
# =============================================================

# [Server]
SERVER_HOST=your-hostname.example.com
SERVER_IPV4=xxx.xxx.xxx.xxx
SERVER_IPV6=xxxx:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx
SERVER_OS=your-server-os (e.g., Ubuntu 22.04)
SERVER_USER=your-server-username
SERVER_PASS=your-server-password

# [SSH: Local -> Server]
SSH_PORT=22522
SSH_KEY_SERVER=./.ssh/id_ed25519
SSH_KEY_SERVER_PUB=./.ssh/id_ed25519.pub

# [SSH: Local/Server -> GitHub]
SSH_KEY_GITHUB=./.ssh/id_ed25519_github
SSH_KEY_GITHUB_PUB=./.ssh/id_ed25519_github.pub

# [GitHub]
GITHUB_USER=your-github-username
GITHUB_EMAIL=your-github-email@example.com

# [Google Account for AI Agent]
GOOGLE_ACCOUNT=your-google-account@gmail.com
GOOGLE_PASSWORD=your-google-password
GOOGLE_2FA_PHONE=0901234xxxx

# =========================================================
# AI Agent Runtime / Verification (added for manifest v3)
# 構築側エージェント向け: 空欄でも良いが「存在を宣言」しておく
# 常駐エージェント向け: 実運用時に必要な値を投入する
# =========================================================

# --- Provider selection ---
# Default provider used by agentd when issue/PR comment does not specify one
AI_PROVIDER_DEFAULT=codex

# Optional fallback order (comma-separated). Example: codex,claude,gemini
AI_PROVIDER_FALLBACK_ORDER=codex,claude,gemini

# Whether provider can be switched per job/issue instruction
AI_PROVIDER_SWITCH_ENABLED=true

# --- API Keys (set only what you use; keep others blank if unused) ---
CODEX_API_KEY=
CLAUDE_API_KEY=
GEMINI_API_KEY=

# Optional endpoint/model overrides (empty = provider defaults)
CODEX_API_BASE_URL=
CODEX_MODEL=
CLAUDE_API_BASE_URL=
CLAUDE_MODEL=
GEMINI_API_BASE_URL=
GEMINI_MODEL=

# --- Browser verification (for reproduce/confirm/fix validation) ---
BROWSER_VERIFY_ENABLED=true

# Choose runtime style used by the server-side agent (examples: headless, headful)
BROWSER_VERIFY_MODE=headless

# Base URL used during browser validation (staging/local tunnel/etc.)
BROWSER_BASE_URL=

# Default browser command/path if needed by your runtime environment
BROWSER_EXECUTABLE_PATH=

# Timeout / retry behavior for browser verification
BROWSER_TIMEOUT_SEC=60
BROWSER_RETRY_COUNT=2

# Require browser verification before "resolved" judgment for UI-related issues
BROWSER_VERIFY_REQUIRED_FOR_UI_ISSUES=true

# --- agentd runtime loop controls (stop conditions support) ---
AGENT_LOOP_MAX_ATTEMPTS=5
AGENT_LOOP_MAX_SAME_FAILURE=2
AGENT_PROGRESS_NOTIFY_ENABLED=true
AGENT_PROGRESS_NOTIFY_INTERVAL_MIN=10

# --- Git workflow behavior (runtime policy support) ---
GIT_SYNC_MAIN_BEFORE_BRANCH=true
GIT_MAIN_BRANCH_NAME=main

# --- Issue / PR protocol hints (optional defaults) ---
ISSUE_DEFAULT_REQUIRE_BROWSER_VERIFY=false
ISSUE_DEFAULT_PROVIDER_HINT=
